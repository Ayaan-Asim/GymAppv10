<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#96D8D8"
    tools:context=".ClassesFragment">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Title -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Classes"
            android:textStyle="bold"
            android:textSize="22sp"
            android:gravity="center"
            android:textColor="#0E0E0E"
            android:layout_marginBottom="12dp" />

        <!-- Search -->
        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/search_bg"
            android:queryHint="Search Class"
            android:layout_marginBottom="16dp" />

        <!-- Filter Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="4"
            android:layout_marginBottom="12dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnClassesToday"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="Today"
                android:textSize="13sp"
                android:textColor="#000000"
                android:backgroundTint="#FFFFFF"
                android:stateListAnimator="@null"
                android:maxLines="1"
                android:ellipsize="end"
                android:includeFontPadding="false"
                app:cornerRadius="12dp" />

            <com.google.android.material.button.MaterialButton

                android:id="@+id/btnClassesTomorrow"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="Tomorrow"
                android:textSize="13sp"
                android:textColor="#000000"
                android:backgroundTint="#FFFFFF"
                android:stateListAnimator="@null"
                android:layout_marginStart="8dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:includeFontPadding="false"
                app:cornerRadius="12dp" />

            <com.google.android.material.button.MaterialButton

                android:id="@+id/btnClassesThisWeek"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="This Week"
                android:textSize="13sp"
                android:textColor="#000000"
                android:backgroundTint="#FFFFFF"
                android:stateListAnimator="@null"
                android:layout_marginStart="8dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:includeFontPadding="false"
                app:cornerRadius="12dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnClassesNextweek"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="Next Week"
                android:textSize="13sp"
                android:textColor="#000000"
                android:backgroundTint="#FFFFFF"
                android:stateListAnimator="@null"
                android:layout_marginStart="8dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:includeFontPadding="false"
                app:cornerRadius="12dp" />
        </LinearLayout>

        <!-- Divider -->
        <View
            android:id="@+id/view2"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="#0E0E0E"
            android:layout_marginBottom="16dp" />

        <!-- Hot Classes -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Hot Classes"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:textColor="#0E0E0E"
            android:layout_marginBottom="8dp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/hotClassesRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:clipToPadding="false"
            android:layout_marginBottom="24dp" />

        <!-- Filtered Classes -->
        <LinearLayout
            android:id="@+id/filteredClassesLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvClassFiltered"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="14sp"
                android:gravity="center"
                android:textColor="#333333"
                android:layout_marginBottom="8dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/filteredClassesRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:clipToPadding="false" />
        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
